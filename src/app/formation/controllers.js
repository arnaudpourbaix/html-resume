(function() {
	'use strict';

	var module = angular.module('htmlResume.formation.controllers', []);

	module.controller('FormationController', [ '$scope', '$logger', function($scope, $logger) {
		var LOGGER = module.name + '.FormationController';
		
	} ]);
	
})();
