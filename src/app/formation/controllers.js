(function() {
	'use strict';

	var module = angular.module('htmlResume.formation.controllers', []);

	module.controller('FormationController', [ '$scope', '$logger', function FormationController($scope, $logger) {
		var LOGGER_NAME = module.name + '.' + this.constructor.name, log = $logger.logger(LOGGER_NAME);
		
	} ]);
	
})();
