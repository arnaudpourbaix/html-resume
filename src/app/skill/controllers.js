(function() {
	'use strict';

	var module = angular.module('htmlResume.skill.controllers', []);

	module.controller('SkillController', [ '$scope', '$logger', function($scope, $logger) {
		var LOGGER = module.name + '.SkillController';
		
	} ]);
	
})();
