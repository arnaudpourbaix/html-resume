(function() {
	'use strict';

	var module = angular.module('htmlResume.experience.controllers', []);

	module.controller('ExperienceController', [ '$scope', '$logger', function($scope, $logger) {
		var LOGGER = module.name + '.ExperienceController';
		
	} ]);
	
})();
