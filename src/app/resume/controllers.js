(function() {
	'use strict';

	var module = angular.module('htmlResume.resume.controllers', []);

	module.controller('ResumeController', [ '$scope', '$rootScope', '$state', '$logger', function($scope, $rootScope, $state, $logger) {
		var LOGGER = module.name + '.ResumeController';
		
	} ]);
	
})();
